import HeroTitle from "./components/HeroTitle";
import { HeroStyled } from "./styled-components/hero.styled";

const Hero = () => {
  return (
    <HeroStyled>
      <HeroTitle />
    </HeroStyled>
  );
};

export default Hero;
